<Window x:Class="EFT_OverlayAPP.OthersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EFT_OverlayAPP"
        mc:Ignorable="d"
        Title="OthersWindow"
        Width="400"
        Height="40"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Loaded="Window_Loaded"
        >
    <Grid>
        <StackPanel Orientation="Horizontal">
            <!-- Open Crafting Window Button -->
            <Button x:Name="OpenCraftingWindowButton"
                    Content="Crafting"
                    Width="100"
                    Height="30"
                    Click="OpenCraftingWindowButton_Click"
                    Margin="0,0,5,0"
                    Background="#55000000"
                    Foreground="White"
                    BorderBrush="Gray"
                    BorderThickness="1"
                    Opacity="0.8">
                <Button.Visibility>
                    <MultiBinding Converter="{StaticResource AdvancedOtherWindowButtonsVisibilityConverter}">
                        <Binding Path = "IsInRaid" />
                        <Binding Path = "HideOtherWindowButtonsWhenInRaid" />
                        <Binding Path = "ManualOtherWindowButtonsVisibilityOverride" />
                </MultiBinding>
                </Button.Visibility>
            </Button>
            <!-- Open Required Items Window Button -->
            <Button x:Name="OpenRequiredItemsWindowButton"
                    Content="Required Items"
                    Width="100"
                    Height="30"
                    Click="OpenRequiredItemsWindowButton_Click"
                    Margin="0,0,5,0"
                    Background="#55000000"
                    Foreground="White"
                    BorderBrush="Gray"
                    BorderThickness="1"
                    Opacity="0.8">
                <Button.Visibility>
                    <MultiBinding Converter="{StaticResource AdvancedOtherWindowButtonsVisibilityConverter}">
                        <Binding Path="IsInRaid" />
                        <Binding Path="HideOtherWindowButtonsWhenInRaid" />
                        <Binding Path="ManualOtherWindowButtonsVisibilityOverride" />
                    </MultiBinding>
                </Button.Visibility>
            </Button>
            <!-- Configuration Button -->
            <Button x:Name="OpenConfigWindowButton"
                    Content="Config"
                    Width="100"
                    Height="30"
                    Click="OpenConfigWindowButton_Click"
                    Background="#55000000"
                    Foreground="White"
                    BorderBrush="Gray"
                    BorderThickness="1"
                    Opacity="0.8">
                <Button.Visibility>
                    <MultiBinding Converter="{StaticResource AdvancedOtherWindowButtonsVisibilityConverter}">
                        <Binding Path="IsInRaid" />
                        <Binding Path="HideOtherWindowButtonsWhenInRaid" />
                        <Binding Path="ManualOtherWindowButtonsVisibilityOverride" />
                    </MultiBinding>
                </Button.Visibility>
            </Button>
        </StackPanel>
    </Grid>
</Window>
